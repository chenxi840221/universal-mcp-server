# GitHub API Token (optional, for project management features)
GITHUB_TOKEN=your_github_token_here

# Database path (optional, defaults to ./data.db)
DATABASE_PATH=./data.db

# Server host and port (optional, defaults to localhost:8000)
SERVER_HOST=localhost
SERVER_PORT=8000

# Debug mode (optional, defaults to False)
DEBUG=False